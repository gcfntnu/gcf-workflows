#-*- mode: snakemake -*-
"""
Snakemake rules for filtering fastq files before analysis.

Fastp used for standard filtering

Filter steps:
-------------

1. Adapter trimming, quality filter (_get_trimmed_R1)


"""

FILTER_INTERIM = join(INTERIM_DIR, 'microbiome', 'filter')


include:
    'filter/fastp.rules'

rule clean_fastq:
    input:
        join(FILTER_INTERIM, 'merged_fastq', 'trimmed', 'fastp', '{sample}_R{readnum}.fastq')
    output:
        join(FILTER_INTERIM, 'cleaned', '{sample}_R{readnum}.fastq')
    shell:
        'cp -fL {input} {output} '

